import React from 'react'

export default function ListPage() {
  // list elixers (name, effect, possible side effect), link to ingredient detail page, (stretch: color code difficulty)
  return (
    <div>ListPage</div>
  )
}
